{
  "name": "diagram-creator-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm query .workspace exec npm test",
    "lint": "npm query .workspace exec npm run lint"
  }
}
